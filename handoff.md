# Handoff

## Current State
Branch `feat/afrho-peak-fits-and-reorg` (contains the unmerged PR #5 and the
activity branch; rebases to nothing once they land).  163 tests.
**The chain finished cleanly** (2026-09-11 19:11): all 68 profiles recentred
(PR #5's re-run is complete), the 12 sparse targets re-queried at V < 21,
7,783 cutout PNGs under `fig/photometry/<T>/cutout/`.

Afρ analysis (`doc/afrho_heliocentric_trends.md`, tables regenerated by
`notebooks/afrho_trends_report.py`): phases at the activity peak, outbursts
excluded, tails set aside, segments where a break is preferred, smoothed
guide curve, SPHEREx windows, grade-D fits not drawn.  `flag_anomalous_bright`
is a critical photometry flag (311 rows).  Two notes answer the physics
questions: `doc/afrho_aperture_systematics.md` (2024 E1's reversal is real;
the 30–40k apertures are sky-dominated) and `doc/afrho_background_annulus.md`
(a local annulus returns m/2 of Afρ — half for 1/ρ — and measures
concentration, not dust).  C/2014 UN271: bright (Tmag 16) but at dec −65 to
−75° — no ZTF data can exist; the r_h cut and the misleading message are fixed.

**The V < 21 re-query changed nothing that matters**: the sparse targets are
sparse because the 10k aperture fails the scale test at large Δ (use their
20k series: 29P 85, 2022R6 54, 2023RS61 31 clean points) or because ZTF
coverage is thin — not because of the magnitude cut.

## Next Steps
1. Merge PR #5 and the activity branch, then PR this branch.
2. Apply the coma-in-annulus factor (1/[1 − ½ ρ_pix/r_med], 8–12%) and an
   advisory `sky_dominated` flag (sky/flux > 10) to the photometry — both
   are calibration decisions, spelled out in the two notes.
3. `figure.ipynb` still writes the old per-target figure names.

## Blind Spots / Dead Ends
- An outburst test against the recent *median* opens on any steep smooth
  rise; compare with the extrapolated trend and require decay (261P is an
  onset, not an outburst).
- BIC on raw χ² calls every kink decisive; inflate errors to χ²_red = 1 on
  the simpler model first.  A kinked line cannot fit a colour step.
- The coma-in-annulus fraction for a median estimator is ½ ρ/r_med with
  r_med² = (r₁² + r₂²)/2, not ρ/(2r₁).
- `df.flags` is a pandas attribute; `--targets` needs designations with
  spaces; a survey run that empties a target's epochs now says which cut.
- The T7 drops under sustained reads; the chain script records each stage's
  exit code, and `survey.py` refuses a pinned-but-missing root.
